<h1>All assignments for all ships:</h1>
<ul id="assignmentList">
<% @assignments.each do |a| %>
    <li><%= a.player.name %> is <%= link_to "assigned", team_assignment_path(a.team, a) %> to the <%= a.team.name %>.</li>
<% end %>
</ul>
<br>

<h2>Create new assignment:</h2>
<%= render 'form' %>
